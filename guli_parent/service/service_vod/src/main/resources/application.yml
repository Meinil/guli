server:
    port: 8003
spring:
    application:
        name: service-vod
    profiles:
        active: dev
    servlet:
        multipart:
            max-file-size: 1024MB # 单文上传大小
            max-request-size: 1024MB # 总文件上传大小
    cloud:
        nacos:
            discovery:
                server-addr: 127.0.0.1:8848

# vod 配置
aliyun:
    vod:
        file:
            keyid:
            keysecret:

